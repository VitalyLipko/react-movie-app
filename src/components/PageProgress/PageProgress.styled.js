import { Grid, styled } from '@mui/material';

export const PageProgressContainer = styled(Grid, {
  name: 'PageProgressContainer',
})({
  height: 100,
  justifyContent: 'center',
  alignItems: 'center',
});
