import { Grid, styled } from '@mui/material';

export const FavoritesStateContainer = styled(Grid, {
  name: 'Favorites-stateContainer',
})({
  height: 300,
  justifyContent: 'center',
  alignItems: 'center',
});
